import { FilterUserOrTextPipe } from './filter-user-or-text.pipe';

describe('FilterUserOrTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserOrTextPipe();
    expect(pipe).toBeTruthy();
  });
});
