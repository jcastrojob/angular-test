import { UserCompanyPipe } from './user-company.pipe';

describe('UserCompanyPipe', () => {
  it('create an instance', () => {
    const pipe = new UserCompanyPipe();
    expect(pipe).toBeTruthy();
  });
});
